<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing 
		infrastructure -->

	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Annotation 기반의 AOP 기능 사용 -->
	<aop:aspectj-autoproxy />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving 
		up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources 
		in the /WEB-INF/views directory -->
	<beans:bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>

	<!-- @Controller 클래스 스캐닝 패키지 -->
	<context:component-scan
		base-package="edu.spring.homeshare" />

	<!-- Interceptor 클래스의 인스턴스를 bean으로 관리 -->
	<!-- 로그인 언터셉터 bean -->
	<beans:bean id="loginInterceptor"
		class="edu.spring.homeshare.interceptor.LoginInterceptor">
	</beans:bean>
	
	<!-- 아이디찾기 bean -->
	<beans:bean id="FindIdInterceptor"
		class="edu.spring.homeshare.interceptor.FindIdInterceptor">
	</beans:bean>
	
	<!-- 회원가입 bean -->
	<beans:bean id="SignUpInterceptor"
		class="edu.spring.homeshare.interceptor.SignUpInterceptor">
	</beans:bean>


	<!-- Interceptor가 가로챌 URL 매핑 설정 -->
	<!-- 로그인 인터셉터 -->
	<interceptors>
		<interceptor>
			<mapping path="/member/login-post" />
			<beans:ref bean="loginInterceptor" />
		</interceptor>
	</interceptors>
	
	<!-- 아이디 찾기 인터셉터-->
	<interceptors>
		<interceptor>
			<mapping path="/member/find-id-post" />
			<beans:ref bean="FindIdInterceptor" />
		</interceptor>
	</interceptors>
	
	<!-- 회원가입 인터셉터-->
	<interceptors>
		<interceptor>
			<mapping path="/member/sign-up-post" />
			<beans:ref bean="SignUpInterceptor" />
		</interceptor>
	</interceptors>
	
	

</beans:beans>










